## Differentiating between encrypted and unencrypted fields

Encrypted fields are suffixed with a single underscore – for example, `email_`. All other fields are assumed to be stored in plain text.
